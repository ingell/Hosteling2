export interface VolunteerProfile {
  firstName: string;
  lastName: string;
  email: string;
  // Add other fields specific to volunteers
}

export interface HostelProfile {
  contactFirstName: string;
  contactLastName: string;
  email: string;
  // Add other fields specific to hostels
}
